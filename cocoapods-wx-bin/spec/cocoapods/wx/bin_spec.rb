# frozen_string_literal: true

RSpec.describe Cocoapods::Wx::Bin do
  it "has a version number" do
    expect(Cocoapods::Wx::Bin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
